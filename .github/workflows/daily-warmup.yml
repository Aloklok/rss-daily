name: Daily & Post-Deploy Warmup

on:
  schedule:
    # 北京时间 00:05 = UTC 16:05 (前一天)
    - cron: '5 16 * * 0' # Weekly on Sunday at 16:05 UTC (00:05 Monday BJ Time)
  # 当 Vercel 部署状态更新时触发
  deployment_status:
  workflow_dispatch:

jobs:
  warmup:
    # 逻辑优化：只在定时任务、手动触发 或 生产环境部署成功时运行
    if: |
      github.event_name == 'schedule' || 
      github.event_name == 'workflow_dispatch' || 
      (github.event_name == 'deployment_status' && 
       github.event.deployment_status.state == 'success' && 
       github.event.deployment.environment == 'Production')

    name: Dual-Region Warmup (US + Asia)
    runs-on: ubuntu-latest
    steps:
      - name: Wait for DNS/CDN Propagation
        if: github.event_name == 'deployment_status'
        run: sleep 60

      # --- 1. 亚洲区域预热 (Vercel API - 日本节点) ---
      - name: Warmup Asia Edge (Vercel API)
        run: |
          echo "🇯🇵 Triggering Asia warmup via Vercel API..."
          # Capture HTTP status and body
          response=$(curl -s -w "\n%{http_code}" "https://www.alok-rss.top/api/system/warmup")

          # Extract body and status (last line is status)
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')

          echo "HTTP Status: $http_code"

          if [ "$http_code" -eq 200 ]; then
            echo "$body" | jq .
            REGION=$(echo "$body" | jq -r '.ingressRegion // "unknown"')
            echo "✅ Warmup completed from edge region: $REGION"
          else
            echo "❌ Warmup Failed:"
            echo "$body"
            exit 1
          fi

      # --- 2. 美国区域预热 (GitHub Runner 直接 Curl) ---
      # 优点:
      # 1. 覆盖全站: 基于 sitemap.xml，包含首页、归档、趋势、所有历史日期
      # 2. 真实模拟: 模拟爬虫行为，从外部通过 sitemap 发现并访问链接
      # 3. 极速轻量: 纯 Shell (curl + awk)，无需安装 Node/pnpm
      - name: Warmup US Edge (Sitemap Strategy)
        run: |
          echo "🇺🇸 Fetching sitemap.xml..."
          START_TIME=$(date +%s%3N)
          SITEMAP_URL="https://www.alok-rss.top/sitemap.xml"

          # 1. 抓取并提取所有 URL
          # 使用 grep 和 sed 提取 xml 中的链接
          URLS=$(curl -s $SITEMAP_URL | grep "<loc>" | sed 's/.*<loc>\(.*\)<\/loc>.*/\1/')
          TOTAL=$(echo "$URLS" | wc -l | xargs)

          echo "Found $TOTAL pages in sitemap."

          # 2. 并行预热 & 收集结果
          # 为了能统计结果，我们需要将并行执行的输出重定向到一个临时文件
          LOG_FILE="warmup_results.log"
          > $LOG_FILE

          echo "$URLS" | while read url; do
            [ -z "$url" ] && continue
            
            # 后台执行 curl
            (
              http_code=$(curl -s -o /dev/null -w "%{http_code}" "$url")
              echo "{\"url\":\"$url\",\"status\":$http_code}" >> $LOG_FILE
            ) &
            
            # 简单的并发控制
            sleep 0.05
          done

          wait

          # 3. 生成统计报告
          END_TIME=$(date +%s%3N)
          DURATION=$((END_TIME - START_TIME))

          SUCCESS_COUNT=$(grep -c '"status":200' $LOG_FILE || true)
          FAILED_COUNT=$(grep -vc '"status":200' $LOG_FILE || true)

          # 构造类似 Vercel API 的 JSON 输出
          # 仅显示前 10 条结果
          RESULTS_PREVIEW=$(head -n 10 $LOG_FILE | jq -c -s '.')

          echo "✅ US Warmup / Sitemap Crawl Done"

          # 使用 jq 构造最终 JSON 并通过 echo 输出
          jq -n \
            --arg msg "Warmed up $SUCCESS_COUNT/$TOTAL pages in ${DURATION}ms" \
            --arg exec "GitHub Runner (US)" \
            --arg ingress "sfo1 (Simulated)" \
            --argjson res "$RESULTS_PREVIEW" \
            --argjson total "$TOTAL" \
            --argjson success "$SUCCESS_COUNT" \
            --argjson failed "$FAILED_COUNT" \
            --argjson time "$DURATION" \
            '{
              success: true,
              message: $msg,
              executionRegion: $exec,
              ingressRegion: $ingress,
              note: "Results truncated to first 10 items for brevity. Check summary for total count.",
              results: $res,
              summary: {
                total: $total,
                success: $success,
                failed: $failed,
                totalTimeMs: $time
              }
            }'
